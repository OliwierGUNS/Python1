# Least common multiple program

# -------------------------
# Subprograms
# -------------------------
def Lcm(num1,num2):
  if num1 > num2:
    x = num1
  else:
    x = num2
  while x % num1 != 0 or x % num2 != 0:
    x = x + 1
  print("the lowest common multiple is",x)
    

# -------------------------
# Main program
# -------------------------
one = int(input("Enter a number\n"))
two = int(input("Enter another number\n"))
Lcm(one,two)
